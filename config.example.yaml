# Canonical configuration: one job produces one final synchronized window.
output_dir: "/path/to/output"
channel_name: "RoomMic2"
keep_intermediates: false

# Shared camera selection inherited by jobs without their own video block.
video:
  kind: directory
  directory: "/path/to/VIDEO"
  camera_serials: ["18486638", "23512014"]

jobs:
  # Raw pair: the paired NS5 header and first packet define UTC.
  - name: "raw-001"
    window: neural
    neural:
      kind: pair
      nev_path: "/path/to/NSP1-001.nev"
      ns5_path: "/path/to/NSP1-001.ns5"
      time_anchor:
        kind: paired_ns5

  # Stitched pair: reference the first raw NEV used by the stitched timeline.
  - name: "stitched-convo"
    window: neural
    neural:
      kind: pair
      nev_path: "/path/to/convo-NSP-1.nev"
      ns5_path: "/path/to/convo-NSP-1.ns5"
      time_anchor:
        kind: first_raw_nev
        reference_path: "/path/to/first-raw.nev"

# Optional shared processing settings:
# ns3_sidecar: true
# gpu_enabled: true
# gpu_type: "nvidia"
